{
  "name": "home-control",
  "version": "1.0.0",
  "description": "Smart home device and scene management",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/server.ts",
    "dev": "ts-node src/server.ts",
    "electron": "electron electron/main.js",
    "electron-dev": "npx concurrently \"cd client && npm run dev\" \"npx wait-on http://localhost:5173 && npm run electron\"",
    "full-dev": "npx concurrently \"npm run dev\" \"cd client && npm run dev\" \"npx wait-on http://localhost:5173 && npm run electron\""
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.18.0",
    "ws": "^8.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "@types/ws": "^8.0.0",
    "concurrently": "^8.0.0",
    "electron": "^40.1.0",
    "electron-builder": "^26.7.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0",
    "wait-on": "^7.0.0"
  }
}
