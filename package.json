{
  "name": "home-control",
  "version": "1.0.0",
  "description": "Smart home device and scene management",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/server.ts",
    "dev": "ts-node --watch src/server.ts",
    "electron": "electron electron/main.js",
    "electron-dev": "concurrently \"cd client && npm run dev\" \"wait-on http://localhost:5173 && npm run electron\"",
    "full-dev": "concurrently \"npm run dev\" \"cd client && npm run dev\" \"wait-on http://localhost:5173 && npm run electron\""
  },
  "dependencies": {
    "express": "^4.18.0",
    "ws": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0",
    "@types/express": "^4.17.0",
    "@types/ws": "^8.0.0",
    "electron": "^28.0.0",
    "electron-builder": "^24.0.0",
    "concurrently": "^8.0.0",
    "wait-on": "^7.0.0"
  }
}